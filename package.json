{
  "name": "jzilg.github.io",
  "version": "4.0.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ./assets/css/style.min.css --minify",
    "dev": "bun run build --watch"
  },
  "devDependencies": {
    "bun-types": "latest",
    "tailwindcss": "^3.4.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
